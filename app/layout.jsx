export default function Layout({ children }) {
    return (
        <html>
            <head>
                <title>LEarn</title>
            </head>
            <body>{children}</body>
        </html>
    )
}